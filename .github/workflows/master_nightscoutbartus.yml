# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - nightscoutbartus

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Log in to registry
      uses: docker/login-action@v1
      with:
        registry: https://index.docker.io/v1/
        username: ${{ secrets.AzureAppService_ContainerUsername_0fdf70046e3f4e3dbddd5a1fc6569053 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_feb40fe906ef43d4855bf47c42bf6c90 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: index.docker.io/${{ secrets.AzureAppService_ContainerUsername_0fdf70046e3f4e3dbddd5a1fc6569053 }}/BartoszJedynak/cgm-remote-monitor:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'nightscoutbartus'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_7b815bcb35044e8ab7302534f6609c77 }}
        images: 'index.docker.io/${{ secrets.AzureAppService_ContainerUsername_0fdf70046e3f4e3dbddd5a1fc6569053 }}/BartoszJedynak/cgm-remote-monitor:${{ github.sha }}'